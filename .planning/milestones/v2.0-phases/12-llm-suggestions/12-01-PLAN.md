---
phase: 12-llm-suggestions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/core/llm_factory.py
  - backend/app/repositories/pricing_repository.py
  - supabase/seed_pricing_history.sql
autonomous: true
requirements:
  - PREC-INF-01
  - PREC-INF-04
  - PREC-INF-05
  - PREC-11

must_haves:
  truths:
    - "LangChain e LangGraph estão instalados no ambiente backend (pip install funciona sem erro)"
    - "create_llm(provider, model, api_key) retorna BaseChatModel para os 3 providers (openai, anthropic, google); provider desconhecido levanta ValueError"
    - "ValueError levantado para api_key vazio/None com mensagem descritiva"
    - "Repositório consegue buscar relatórios de diagnóstico associados a um projeto"
    - "Repositório consegue buscar top-3 pricings aprovados por project_type"
    - "Repositório consegue inserir múltiplas features em lote (bulk_insert_features)"
    - "pricing_history contém pelo menos 7 snapshots de projetos aprovados com project_type preenchido"
  artifacts:
    - path: "backend/app/core/llm_factory.py"
      provides: "Fábrica de BaseChatModel por provider/model/api_key"
      exports: ["create_llm"]
    - path: "backend/app/repositories/pricing_repository.py"
      provides: "Métodos de consulta estendidos: get_project_reports, get_top_history_by_type, bulk_insert_features"
    - path: "supabase/seed_pricing_history.sql"
      provides: "7+ snapshots realistas de precificações aprovadas na pricing_history"
  key_links:
    - from: "backend/app/services/llm_pricing_service.py (Wave 2)"
      to: "backend/app/core/llm_factory.py"
      via: "create_llm(provider, model, api_key)"
      pattern: "from app.core.llm_factory import create_llm"
    - from: "backend/app/services/llm_pricing_service.py (Wave 2)"
      to: "backend/app/repositories/pricing_repository.py"
      via: "get_project_reports, get_top_history_by_type, bulk_insert_features"
      pattern: "self._repo\\.(get_project_reports|get_top_history_by_type|bulk_insert_features)"
---

<objective>
Instalar infraestrutura LLM (LangChain/LangGraph), criar a fábrica de modelos, estender o repositório com os métodos necessários para as sugestões, e popular a pricing_history com dados de seed realistas.

Purpose: O Wave 2 (llm_pricing_service) depende da fábrica de LLM, dos 3 métodos de repositório novos, e de dados de seed para testar a busca por histórico por project_type. Nada disso existe ainda.

Output: requirements.txt atualizado, llm_factory.py funcional, 3 novos métodos no PricingRepository, script SQL de seed com 7+ snapshots.
</objective>

<execution_context>
@/Users/gabrielmezzalira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gabrielmezzalira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-precificador-foundation/11-01-SUMMARY.md
@.planning/phases/11-precificador-foundation/11-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos existentes que este plano estende. Leia antes de implementar. -->

De backend/app/repositories/pricing_repository.py (classe PricingRepository existente):
  - __init__(self, db: Client) -> None
  - get_project(project_id: str) -> dict | None  — seleciona id, project_type
  - list_pricings(project_id: str) -> list[dict]
  - insert_pricing(row: dict) -> dict
  - get_pricing(pricing_id: str) -> dict | None
  - update_pricing(pricing_id: str, updates: dict) -> dict | None
  - delete_pricing(pricing_id: str) -> None
  - update_pricing_status(pricing_id: str, status: str) -> dict | None
  - insert_history(row: dict) -> dict
  - list_pricing_history(project_id: str) -> list[dict]
  - get_pricing_features(pricing_id: str) -> list[dict]
  - insert_feature(row: dict) -> dict
  - get_feature(feature_id: str, pricing_id: str) -> dict | None
  - update_feature(feature_id: str, updates: dict) -> dict | None
  - delete_feature(feature_id: str) -> None

De backend/app/routers/projects.py (padrão de vault decrypt):
  - db.rpc("vault_create_secret", {"p_secret": secret, "p_name": name}).execute()
  - db.rpc("vault_delete_secret", {"p_secret_id": secret_id}).execute()
  Para DECRYPT (leitura): db.rpc("vault.decrypted_secrets", {}).execute() retorna
  rows com {id, secret, name}. Filtrar por .eq("id", secret_id).

Schema da tabela reports (para get_project_reports):
  - id UUID, session_id UUID FK sessions, markdown_content TEXT, cost_usd NUMERIC, generated_at TIMESTAMPTZ

Schema da tabela sessions (para join com reports):
  - id UUID, project_id UUID FK projects, ...

Schema da tabela pricing_history (para get_top_history_by_type):
  - id UUID, project_id UUID, pricing_id UUID, snapshot JSONB, approved_at TIMESTAMPTZ
  - snapshot.project_type: str — campo usado como filtro

Formato do snapshot JSONB em pricing_history (confirmado pelo 11-01-SUMMARY):
{
  "project_type": "bi",
  "inputs": { "start_date": "...", "num_analysts": 3, "hours_per_day": 3, "ticket_price": 10000, "extra_calendar_days": 0 },
  "features": [{ "bloco": "...", "funcionalidade": "...", "horas": 15 }],
  "outputs": { "preco_total": "12000", "dias_corridos": "36", ... }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Instalar dependências LangChain e criar llm_factory.py</name>
  <files>backend/requirements.txt, backend/app/core/llm_factory.py</files>
  <read_first>
    - backend/requirements.txt — versão atual das deps; adicionar logo abaixo de google-genai
    - backend/app/core/pricing_calculator.py — padrão de estrutura de arquivo em core/ (imports, classe pura, sem side effects)
    - .planning/phases/11-precificador-foundation/11-PATTERNS.md — seção "llm_factory.py" (linhas 444-479) — padrão factory exato
    - CLAUDE.md — seção "Regra de frameworks de IA por módulo" e "Configurabilidade de modelo no Precificador"
  </read_first>
  <action>
    Em backend/requirements.txt: adicionar as seguintes linhas ao final do arquivo (após google-genai):
      langchain>=0.3.0
      langchain-openai>=0.2.0
      langchain-anthropic>=0.3.0
      langchain-google-genai>=2.0.0
      langgraph>=0.2.0

    Criar backend/app/core/llm_factory.py com a função pública create_llm(provider: str, model: str, api_key: str) -> BaseChatModel:
    - Import do tipo abstrato: from langchain_core.language_models import BaseChatModel
    - Validação inicial: se api_key estiver vazio ou None, levantar ValueError("api_key is required to create an LLM client")
    - Usar match/case por provider:
      - "openai": importar ChatOpenAI de langchain_openai; retornar ChatOpenAI(model=model, api_key=api_key)
      - "anthropic": importar ChatAnthropic de langchain_anthropic; retornar ChatAnthropic(model=model, api_key=api_key)
      - "google": importar ChatGoogleGenerativeAI de langchain_google_genai; retornar ChatGoogleGenerativeAI(model=model, google_api_key=api_key)
      - case _: levantar ValueError(f"Unknown LLM provider: '{provider}'. Supported values: 'openai', 'anthropic', 'google'.")
    - Imports concretos ficam DENTRO dos cases (lazy import) — nunca no topo do arquivo. Isso garante que o arquivo não falha ao importar caso apenas um pacote esteja instalado.
    - O tipo de retorno da função na assinatura é BaseChatModel (abstração) — nunca o tipo concreto.
    - Docstring breve: "Factory que retorna BaseChatModel a partir de provider/model/api_key. O restante do código só conhece BaseChatModel, nunca o provider concreto."
  </action>
  <verify>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Agente Diagnotico/backend" && python -c "from app.core.llm_factory import create_llm; print('OK')"</automated>
  </verify>
  <done>
    - requirements.txt contém as 5 linhas langchain* e langgraph
    - create_llm importa sem erro
    - ValueError levantado para provider desconhecido (verificar com: python -c "from app.core.llm_factory import create_llm; create_llm('unknown', 'x', 'y')" retorna mensagem de erro descritiva)
    - ValueError levantado para api_key vazio
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Estender PricingRepository com 3 novos métodos + criar seed SQL</name>
  <files>backend/app/repositories/pricing_repository.py, supabase/seed_pricing_history.sql</files>
  <read_first>
    - backend/app/repositories/pricing_repository.py — arquivo completo; os 3 métodos novos são adicionados ao final da classe
    - .planning/phases/11-precificador-foundation/11-CONTEXT.md — seção "Historical pricing snapshot format" para o formato exato do JSONB
    - CLAUDE.md — seção Schema do Banco de Dados (tabelas reports, sessions, pricing_history)
  </read_first>
  <action>
    Em backend/app/repositories/pricing_repository.py, adicionar 3 métodos ao final da classe PricingRepository (após delete_feature):

    1. get_project_reports(self, project_id: str) -> list[dict]:
       - Buscar sessions onde project_id = project_id, pegar apenas ids: db.table("sessions").select("id").eq("project_id", project_id).execute()
       - Se não houver sessions, retornar []
       - Coletar session_ids: [s["id"] for s in sessions.data]
       - Buscar reports onde session_id IN session_ids: db.table("reports").select("id, markdown_content, generated_at").in_("session_id", session_ids).order("generated_at", desc=True).execute()
       - Retornar result.data or []

    2. get_top_history_by_type(self, project_type: str, limit: int = 3) -> list[dict]:
       - Buscar em pricing_history onde snapshot->>'project_type' = project_type
       - Usar filtro PostgREST: .filter("snapshot->>project_type", "eq", project_type)
       - Ordenar por approved_at desc, limitar a `limit` registros
       - Retornar result.data or []

    3. bulk_insert_features(self, pricing_id: str, features: list[dict]) -> list[dict]:
       - Se features estiver vazio, retornar []
       - Preparar rows: cada item recebe "pricing_id": pricing_id; os campos bloco, funcionalidade, horas já vêm nos dicts de entrada
       - Inserir em lote: db.table("pricing_features").insert(rows).execute()
       - Retornar result.data or []

    Criar supabase/seed_pricing_history.sql com pelo menos 7 INSERTs na tabela pricing_history.
    Cada INSERT usa um project_id fictício fixo (UUID válido, ex: '00000000-0000-0000-0000-000000000001'), pricing_id fictício, e snapshot JSONB completo.
    Os 7 registros devem cobrir project_types variados:
      - 2 registros de project_type "bi" (dashboards de vendas e financeiro)
      - 2 de "data_engineering" (pipeline ETL e ingestão de dados)
      - 1 de "ml" (modelo de churn)
      - 1 de "automation" (automação de relatórios)
      - 1 de "integration" (integração ERP-CRM)
    Cada snapshot deve ter features realistas com blocos e funcionalidades em português (ex: "Engenharia de Dados" / "Ingestão de dados do ERP" / horas: 20).
    Adicionar no topo do arquivo um comentário: "-- Seed: 7 approved pricings for LLM history context (Phase 12). Run manually in Supabase SQL editor."
    Usar ON CONFLICT DO NOTHING para idempotência.
    Não criar os project_ids e pricing_ids como FKs reais — usar UUIDs fixos e adicionar comentário explicando que são dados de seed isolados sem FK real.
  </action>
  <verify>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Agente Diagnotico/backend" && python -c "from app.repositories.pricing_repository import PricingRepository; import inspect; methods = [m for m in dir(PricingRepository) if not m.startswith('_')]; assert 'get_project_reports' in methods; assert 'get_top_history_by_type' in methods; assert 'bulk_insert_features' in methods; print('OK - 3 methods found')"</automated>
  </verify>
  <done>
    - Os 3 métodos existem e são importáveis sem erro
    - supabase/seed_pricing_history.sql existe com pelo menos 7 blocos INSERT
    - Cada snapshot no SQL tem "project_type" preenchido no JSONB e features com bloco/funcionalidade/horas
    - get_top_history_by_type usa filtro "snapshot->>project_type" (não filtragem Python-side)
    - bulk_insert_features faz insert em lote (um único db.table().insert(rows) com lista, não loop)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → llm_factory | api_key decriptado do Vault passa por aqui; nunca logar ou expor em resposta |
| seed SQL → Supabase | Dados fictícios; UUIDs de seed não devem colidir com dados reais |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | llm_factory.py | mitigate | api_key nunca logado; ValueError mensagem não inclui o valor da chave |
| T-12-02 | Tampering | requirements.txt (novos pacotes) | mitigate | Verificar manualmente no PyPI antes de instalar; versões pinadas com >= mínimo |
| T-12-SC | Tampering | npm/pip installs | mitigate | Pacotes LangChain são amplamente conhecidos; confirmar no PyPI se [ASSUMED] |
</threat_model>

<verification>
Após execução do plano:

1. pip install -r requirements.txt — deve completar sem erro com os pacotes langchain* e langgraph
2. python -c "from app.core.llm_factory import create_llm; from langchain_core.language_models import BaseChatModel; print('LLMFactory OK')"
3. python -c "from app.repositories.pricing_repository import PricingRepository; r = PricingRepository.__dict__; assert 'get_project_reports' in r; assert 'get_top_history_by_type' in r; assert 'bulk_insert_features' in r; print('Repo OK')"
4. ls supabase/seed_pricing_history.sql e grep -c "INSERT INTO pricing_history" supabase/seed_pricing_history.sql — deve retornar >= 7
</verification>

<success_criteria>
- requirements.txt inclui langchain, langchain-openai, langchain-anthropic, langchain-google-genai, langgraph
- backend/app/core/llm_factory.py existe com create_llm(provider, model, api_key) -> BaseChatModel
- PricingRepository tem 3 novos métodos: get_project_reports, get_top_history_by_type, bulk_insert_features
- supabase/seed_pricing_history.sql tem >= 7 INSERTs com project_type variados e features realistas
</success_criteria>

<output>
Criar .planning/phases/12-llm-suggestions/12-01-SUMMARY.md ao concluir
</output>

---
phase: "01-project-configuration-supabase-schema"
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01a"
  - "01-01b"
  - "01-02"
files_modified:
  - frontend/src/api/projects.ts
  - frontend/src/types/project.ts
  - frontend/src/lib/supabase.ts
  - frontend/src/pages/HomePage.tsx
  - frontend/src/pages/ProjectFormPage.tsx
  - frontend/src/pages/ProjectDetailPage.tsx
  - frontend/src/components/ProjectCard.tsx
  - frontend/src/components/ProjectTypeSelector.tsx
  - frontend/src/components/DmsSlider.tsx
  - frontend/src/components/Topbar.tsx
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04

must_haves:
  truths:
    - "User can open the home page and see a card grid of existing projects with correct fields (name, client, type badge, ao vivo badge if active)"
    - "User can navigate to /projects/new, fill the form with all 11 fields, submit, and be redirected to home where the new project card appears"
    - "User can navigate to /projects/:id/edit, see fields pre-filled, change the project name, submit, and see the updated name on home"
    - "In edit mode, the Gemini API key field shows 10 bullet dots (disabled), and Alterar chave button re-enables it"
    - "Form validation shows Portuguese error messages before submit (name required, project type required, API key required)"
    - "All design tokens from the UI-SPEC are applied — no hardcoded hex values in components"
  artifacts:
    - path: "frontend/src/types/project.ts"
      provides: "TypeScript interfaces for ProjectCreate, ProjectResponse"
      exports: ["ProjectCreate", "ProjectResponse"]
      contains: "has_api_key: boolean"
    - path: "frontend/src/api/projects.ts"
      provides: "fetch() wrappers for all 5 project endpoints"
      exports: ["projectsAPI"]
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "Project card grid with empty state and ao vivo badge"
      contains: "ao vivo"
    - path: "frontend/src/pages/ProjectFormPage.tsx"
      provides: "Create + edit form with all 11 fields"
      contains: "Alterar chave"
    - path: "frontend/src/components/ProjectCard.tsx"
      provides: "Project card component"
      contains: "has_active_session"
    - path: "frontend/src/components/ProjectTypeSelector.tsx"
      provides: "6-item type selector pills"
    - path: "frontend/src/components/DmsSlider.tsx"
      provides: "1–5 slider with level labels"
  key_links:
    - from: "frontend/src/pages/ProjectFormPage.tsx"
      to: "frontend/src/api/projects.ts"
      via: "projectsAPI.create() and projectsAPI.update()"
      pattern: "projectsAPI\\.(create|update)"
    - from: "frontend/src/pages/HomePage.tsx"
      to: "frontend/src/api/projects.ts"
      via: "projectsAPI.list()"
      pattern: "projectsAPI\\.list"
    - from: "frontend/src/components/ProjectCard.tsx"
      to: "frontend/src/types/project.ts"
      via: "ProjectResponse type"
      pattern: "ProjectResponse"
---

<objective>
Build the complete React UI for Phase 1: TypeScript types, API client, three pages (HomePage, ProjectFormPage, ProjectDetailPage), and three custom components (ProjectCard, ProjectTypeSelector, DmsSlider, Topbar). All design tokens from the UI-SPEC are applied via Tailwind v4 utility classes derived from the @theme tokens defined in Plan 01b.

Purpose: Completes the Walking Skeleton. After this plan, the full vertical slice works: user fills form → POST /api/projects → Supabase insert → project card appears on home screen.

Output:
- frontend/src/types/project.ts — TypeScript interfaces
- frontend/src/api/projects.ts — typed fetch() wrappers
- frontend/src/lib/supabase.ts — browser Supabase client (placeholder, not used for data)
- frontend/src/pages/HomePage.tsx — card grid with empty state
- frontend/src/pages/ProjectFormPage.tsx — create + edit form with all 11 fields
- frontend/src/pages/ProjectDetailPage.tsx — placeholder detail page
- frontend/src/components/ProjectCard.tsx — project card
- frontend/src/components/ProjectTypeSelector.tsx — 6-item type selector
- frontend/src/components/DmsSlider.tsx — 1-5 slider with labels
- frontend/src/components/Topbar.tsx — sticky navigation topbar
</objective>

<execution_context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/phases/01-project-configuration-supabase-schema/01-UI-SPEC.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/phases/01-project-configuration-supabase-schema/01-RESEARCH.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/phases/01-project-configuration-supabase-schema/01-02-SUMMARY.md
</execution_context>

<context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/ROADMAP.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/phases/01-project-configuration-supabase-schema/01-CONTEXT.md

<interfaces>
<!-- TypeScript contracts and Tailwind token usage -->

ProjectResponse interface (mirrors Plan 02 Pydantic model):
```typescript
export interface ProjectResponse {
  id: string
  name: string
  client: string
  description?: string
  project_type: 'bi' | 'ml' | 'data_engineering' | 'automation' | 'integration' | 'science'
  budget_usd?: number
  data_maturity_score: number  // 1–5
  pre_meeting_context?: string
  meeting_url?: string
  source: 'taqtic' | 'recall'
  question_ttl_seconds: number
  has_api_key: boolean    // PROJ-04 — never the key itself
  has_active_session: boolean  // PROJ-03 — drives "ao vivo" badge
  sessions: unknown[]     // Empty array in Phase 1; populated in Phase 2
  created_at?: string
  updated_at?: string
}
```

Tailwind v4 token classes (from @theme defined in Plan 01b index.css):
- bg-bg-page, bg-surface, bg-green-bg-light, bg-green-bg-tag, bg-red-bg, bg-yellow-bg, bg-muted
- border-border-std, border-border-hover, border-border-green, border-border-red, border-border-yellow
- text-text-primary, text-text-secondary, text-accent, text-red, text-yellow
- rounded-card (10px), rounded-panel (8px), rounded-input (7px), rounded-tag (4px), rounded-pill (99px), rounded-btn (7px)
- h-topbar (52px), h-budget-bar (36px), h-btn (34px)
- font-sans, font-mono

UI-SPEC Copywriting Contract (use exact Portuguese strings — see 01-UI-SPEC.md):
- Empty state heading: "Nenhum projeto ainda"
- Empty state body: "Crie o primeiro projeto para começar um diagnóstico."
- Form sections: "IDENTIFICAÇÃO", "CONFIGURAÇÃO DE IA", "REUNIÃO", "MATURIDADE"
- API key label: "Chave de API Gemini"
- API key helper: "Armazenada com segurança. Nunca exibida após salvar."
- API key change link: "Alterar chave"
- Edit disabled placeholder: "●●●●●●●●●●" (10 U+25CF characters)
- Submit (create): "Criar projeto" | Submit (edit): "Salvar alterações"
- Cancel: "Descartar"
- Delete: "Excluir projeto"
- Active badge: "ao vivo"

Project type items (ProjectTypeSelector):
- BI → project_type='bi'
- ML → project_type='ml'
- Eng. de Dados → project_type='data_engineering'
- Automação → project_type='automation'
- Integração → project_type='integration'
- Ciência de Dados → project_type='science'

DMS levels for slider labels:
1=Inicial, 2=Gerenciado, 3=Definido, 4=Quantificado, 5=Otimizado
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types + API client + Supabase lib placeholder</name>
  <files>
    frontend/src/types/project.ts,
    frontend/src/api/projects.ts,
    frontend/src/lib/supabase.ts
  </files>
  <read_first>
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/phases/01-project-configuration-supabase-schema/01-PATTERNS.md (Pattern assignments for frontend/src/api/projects.ts and frontend/src/lib/supabase.ts — full code excerpts)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/phases/01-project-configuration-supabase-schema/01-RESEARCH.md (Anti-Patterns: SUPABASE_KEY never in browser; frontend always goes through FastAPI)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/backend/app/models/project.py (read existing Pydantic models to match TypeScript types exactly)
  </read_first>
  <action>
    Create frontend/src/types/project.ts with the ProjectCreate, ProjectUpdate, and ProjectResponse TypeScript interfaces exactly matching the Pydantic models from Plan 02. Include the project_type and source union type literals. ProjectResponse must include `sessions: unknown[]` (empty array field matching the backend response).

    Create frontend/src/api/projects.ts with a typed `projectsAPI` object containing 5 methods: list, create, getById, update, delete. Use the `fetchJSON<T>` helper pattern from RESEARCH.md Pattern assignment. All calls go to `/api/projects` (prefix stripped by Vite proxy in dev). All errors converted to thrown Error with `error.detail || "HTTP ${status}"`.

    Create frontend/src/lib/supabase.ts as a placeholder. Include a comment: "This client is NOT used in Phase 1 — all Supabase access is server-side through FastAPI. Placeholder for future real-time subscriptions." The file should export a `getSupabaseClient()` function that reads `import.meta.env.VITE_SUPABASE_URL` and `import.meta.env.VITE_SUPABASE_ANON_KEY` (NOT the service role key) — but do NOT call this function anywhere in Phase 1.

    For the delete method: `async delete(id: string): Promise<void>` — calls `fetchJSON<void>` with method: 'DELETE'. The helper should handle 204 (no content) responses without trying to parse JSON.

    Do NOT create a frontend/.env file with SUPABASE_KEY — that key must stay server-side only.
  </action>
  <verify>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente\ Diagnotico/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/project.ts exports ProjectCreate, ProjectUpdate, ProjectResponse interfaces
    - ProjectResponse interface contains `has_api_key: boolean` and `has_active_session: boolean`
    - ProjectResponse interface contains `sessions: unknown[]` (matching backend response shape)
    - ProjectResponse interface does NOT contain `gemini_api_key` field
    - frontend/src/api/projects.ts exports `projectsAPI` with list, create, getById, update, delete methods
    - All API methods use typed generics: `fetchJSON<ProjectResponse>`, `fetchJSON<ProjectResponse[]>`, etc.
    - `npx tsc --noEmit` exits 0 after creating these files (no TypeScript errors)
    - frontend/src/lib/supabase.ts does NOT use VITE_SUPABASE_KEY (only VITE_SUPABASE_ANON_KEY if anything)
  </acceptance_criteria>
  <done>TypeScript types match the backend API contract. API client is typed and uses the Vite proxy path. Supabase browser lib is a non-functional placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Custom components — ProjectCard, ProjectTypeSelector, DmsSlider, Topbar</name>
  <files>
    frontend/src/components/ProjectCard.tsx,
    frontend/src/components/ProjectTypeSelector.tsx,
    frontend/src/components/DmsSlider.tsx,
    frontend/src/components/Topbar.tsx
  </files>
  <read_first>
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/phases/01-project-configuration-supabase-schema/01-UI-SPEC.md (Component Specifications section — exact pixel specs for ProjectCard, ProjectTypeSelector, DmsSlider, Page Topbar; Color section for exact usage rules; Copywriting Contract for "ao vivo" badge text)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/phases/01-project-configuration-supabase-schema/01-CONTEXT.md (Design System section — all component specs; Components subsection)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/frontend/src/types/project.ts (just created — use ProjectResponse type for ProjectCard props)
  </read_first>
  <action>
    Create four React components. Use ONLY Tailwind v4 utility classes derived from the @theme tokens — no hardcoded hex values, no inline style={{ color: '#22a267' }}.

    **ProjectCard.tsx:**
    Props: `{ project: ProjectResponse; onClick?: () => void }`
    Layout per UI-SPEC:
    - Outer div: bg-surface, border border-border-std, rounded-card (rounded-[10px]), p-4 (16px), cursor-pointer, transition-colors duration-150
    - hover: border-border-hover, shadow (box-shadow: 0 2px 8px rgba(0,0,0,0.06))
    - Row 1: project.name (text-sm font-medium text-text-primary) + type badge right-aligned
    - Row 2: project.client (text-xs text-text-secondary, mt-1)
    - Row 3 (conditional — project.has_active_session): "ao vivo" badge — bg-accent text-white text-[10px] font-medium uppercase px-2 py-0.5 rounded-pill (mt-2)
    - Row 4: created_at date (text-xs text-text-secondary, mt-2)
    - Type badge: bg-muted text-text-primary text-[10px] font-medium uppercase px-2 py-0.5 rounded-tag
    - Use human-readable type label map: bi→BI, ml→ML, data_engineering→Eng. de Dados, automation→Automação, integration→Integração, science→Ciência de Dados

    **ProjectTypeSelector.tsx:**
    Props: `{ value: string; onChange: (type: string) => void; error?: boolean }`
    - 6 pill items in a flex-wrap row, gap-2
    - Each pill: bg-surface border border-border-std rounded-panel p-2.5 cursor-pointer flex items-center gap-2 text-xs font-medium transition-colors duration-150
    - Selected: border-2 border-accent bg-green-bg-light
    - Unselected hover: border-border-hover
    - Error (none selected + error=true): wrapper gets border border-red rounded-panel p-1 (highlight entire group)
    - Icons from lucide-react (16px): use BarChart2 for BI, Brain for ML, Database for Eng. de Dados, Zap for Automação, Link for Integração, FlaskConical for Ciência de Dados
    - Labels: BI, ML, Eng. de Dados, Automação, Integração, Ciência de Dados

    **DmsSlider.tsx:**
    Props: `{ value: number; onChange: (val: number) => void }`
    - HTML range input: min=1, max=5, step=1
    - Track styling: h-1 (4px), bg-muted, rounded-pill — apply via CSS class since native range is hard to style with Tailwind
    - Thumb: bg-accent, 18px circle — apply via `accent-color: var(--color-accent)` in index.css (or inline style for the thumb)
    - Labels row: flex justify-between, 5 spans for "1\nInicial" through "5\nOtimizado"
    - Label font: text-[10px] text-text-secondary; Active label (current value): text-[10px] font-medium text-text-primary
    - Default value: 3
    - Note: native range input in browsers will use accent-color for thumb color — add `accent-color: var(--color-accent)` to the input's className via inline style or a global CSS rule in index.css

    **Topbar.tsx:**
    Props: `{ children?: React.ReactNode }` (flexible right slot)
    - height h-topbar (52px, defined in @theme), bg-surface, border-b border-border-std, px-6, sticky top-0 z-50
    - flex items-center justify-between
    - Left: "Agente Diagnóstico" — text-sm font-medium text-text-primary
    - Right: children slot (home page puts "Criar projeto" button here)
  </action>
  <verify>
    <!-- Note: Task 2 verify cannot deeply type-check components because consuming pages are
         placeholders at this stage. This is acceptable — Task 3 build is the real gate.
         The tsc check here validates component type signatures in isolation. -->
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente\ Diagnotico/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 4 component files exist in frontend/src/components/
    - ProjectCard.tsx renders the "ao vivo" badge conditionally based on `project.has_active_session`
    - ProjectCard.tsx uses Tailwind classes — no hardcoded hex values (grep -r "#22a267\|#f5f4f0\|#1a1a1a" frontend/src/components/ returns 0 matches)
    - ProjectTypeSelector.tsx renders exactly 6 items with the correct project_type values
    - DmsSlider.tsx renders a range input with min=1, max=5, step=1 and 5 labels
    - `npx tsc --noEmit` exits 0 (no TypeScript errors in component files)
    - Topbar.tsx renders at height 52px (h-topbar class applied)
  </acceptance_criteria>
  <done>Four reusable components built to UI-SPEC. No hardcoded colors. TypeScript types clean.</done>
</task>

<task type="auto">
  <name>Task 3: Pages — HomePage, ProjectFormPage (create + edit), ProjectDetailPage placeholder</name>
  <files>
    frontend/src/pages/HomePage.tsx,
    frontend/src/pages/ProjectFormPage.tsx,
    frontend/src/pages/ProjectDetailPage.tsx
  </files>
  <read_first>
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/phases/01-project-configuration-supabase-schema/01-UI-SPEC.md (full file — Project Grid, Form Layout, Form Field, API Key Field, Radio Group, Numeric Input, Action Buttons, Copywriting Contract, Error State Copies)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/.planning/phases/01-project-configuration-supabase-schema/01-CONTEXT.md (Project Form UX section — sections order, home card grid, API Key masking UX)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/frontend/src/api/projects.ts (just created — methods to call)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/frontend/src/types/project.ts (interfaces to use)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/frontend/src/components/ProjectCard.tsx (import and use)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/frontend/src/components/ProjectTypeSelector.tsx (import and use)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/frontend/src/components/DmsSlider.tsx (import and use)
    - /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente Diagnotico/frontend/src/components/Topbar.tsx (import and use)
  </read_first>
  <action>
    Build the three pages. Replace the placeholder shells from Plan 01b.

    **HomePage.tsx:**
    - On mount: call `projectsAPI.list()` → setProjects(data). Show loading spinner while fetching. Show error message "Não foi possível carregar os projetos. Tente recarregar a página." on fetch failure.
    - Layout: Topbar (right slot: "Criar projeto" primary button linking to /projects/new), then a main area with grid
    - Grid per UI-SPEC: `grid grid-cols-[repeat(auto-fill,minmax(280px,1fr))] gap-4 p-6 max-w-5xl mx-auto`
    - Render ProjectCard for each project. onClick → navigate to `/projects/${project.id}`
    - Empty state (projects.length === 0, not loading): centered div, pt-16, heading "Nenhum projeto ainda" (text-sm font-medium text-text-primary), body "Crie o primeiro projeto para começar um diagnóstico." (text-xs text-text-secondary mt-1), primary "Criar projeto" button below

    **ProjectFormPage.tsx:**
    - Detect create vs edit: `const { id } = useParams(); const isEdit = Boolean(id)`
    - In edit mode, fetch project via `projectsAPI.getById(id!)` on mount and pre-fill all fields
    - Form state: use useState for each field or a single formData object — pick whichever is cleaner
    - Form sections in this order (per UI-SPEC):
      1. IDENTIFICAÇÃO — name (text, required), client (text, required), description (textarea, optional), ProjectTypeSelector
      2. CONFIGURAÇÃO DE IA — Gemini API key field (see masking logic below), budget_usd (number, optional, placeholder "Sem limite", helper "USD"), question_ttl_seconds (number, optional, placeholder "30", helper "segundos")
      3. REUNIÃO — meeting_url (text, optional, placeholder "https://meet.google.com/..."), source radio group (taqtic/recall)
      4. MATURIDADE — DmsSlider (label: "Maturidade de dados do cliente")

    API Key masking:
    - State: `const [keyMode, setKeyMode] = useState<'enter' | 'masked' | 'editing'>('enter')`
    - 'enter' (create mode): `<input type="password" />` + Eye/EyeOff toggle (Lucide Eye/EyeOff icons, 16px, text-text-secondary)
    - 'masked' (edit mode, key not being changed): `<input disabled value="●●●●●●●●●●" />` (bg-bg-page text-text-secondary cursor-not-allowed) + "Alterar chave" link below (text-accent text-sm font-medium, hover:underline, onClick: setKeyMode('editing'))
    - 'editing' (edit mode, changing key): standard password input, focused. Include "Cancelar" link that resets back to 'masked'
    - When isEdit and project loads: setKeyMode('masked'); the gemini_api_key field in formData starts empty

    Validation (client-side, on submit):
    - name empty → "Nome do projeto é obrigatório."
    - client empty → "Nome do cliente é obrigatório."
    - project_type not selected → "Selecione o tipo de projeto."
    - gemini_api_key empty in create mode → "A chave de API Gemini é obrigatória."
    - budget_usd < 0 → "O valor deve ser maior que zero, ou deixe em branco para sem limite."
    - question_ttl_seconds < 10 → "O TTL mínimo é 10 segundos."
    - Show each error below its field (text-xs text-red mt-1). Input gets border-red focus:border-red on error.

    On submit (valid):
    - Create: `projectsAPI.create(formData)` → navigate to `/` on success
    - Edit: `projectsAPI.update(id!, formData)` → navigate to `/projects/${id}` on success
    - Only include gemini_api_key in the PUT body if keyMode === 'editing' and the key field is non-empty
    - Show "Erro ao salvar o projeto. Verifique sua conexão e tente novamente." on API failure

    Form footer: primary submit button ("Criar projeto" or "Salvar alterações"), secondary cancel button ("Descartar" → navigate back). In edit mode: "Excluir projeto" danger button — on click, confirm dialog then `projectsAPI.delete(id!)` → navigate to `/`.

    Form max-width: max-w-lg (640px) mx-auto p-6.

    Section headers: uppercase text-[10px] font-medium tracking-widest text-text-secondary border-b border-border-std pb-2 mb-3 mt-8 first:mt-0

    **ProjectDetailPage.tsx:**
    - Fetch project by id via `projectsAPI.getById(id!)`
    - Show project name and client as heading
    - Placeholder text: "Sessões e histórico estarão disponíveis na Fase 2."
    - Link back to home: "← Projetos"
    - Link to edit: "Editar projeto" secondary button linking to `/projects/${id}/edit`

    Topbar on all pages: use Topbar component.
  </action>
  <verify>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/DataInsight/Agente\ Diagnotico/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/HomePage.tsx calls projectsAPI.list() and renders ProjectCard for each result
    - frontend/src/pages/HomePage.tsx renders "Nenhum projeto ainda" empty state when array is empty
    - frontend/src/pages/ProjectFormPage.tsx has all 4 form sections (IDENTIFICAÇÃO, CONFIGURAÇÃO DE IA, REUNIÃO, MATURIDADE)
    - frontend/src/pages/ProjectFormPage.tsx shows 10 bullet dots in edit mode for API key (grep "●●●●●●●●●●" returns a match)
    - frontend/src/pages/ProjectFormPage.tsx shows "Alterar chave" text in edit mode
    - frontend/src/pages/ProjectFormPage.tsx shows all Portuguese error messages on submit with empty required fields
    - frontend/src/pages/ProjectDetailPage.tsx renders project name and edit link
    - `npm run build` exits 0 — no TypeScript errors, no Tailwind errors
    - No hardcoded hex values in any page file: `grep -rE "#[0-9a-fA-F]{3,6}" frontend/src/pages/` returns 0 matches
    - "ao vivo" text appears in ProjectCard (rendered by HomePage when has_active_session=true)
  </acceptance_criteria>
  <done>All three pages are functional. The full vertical slice works: ProjectFormPage → projectsAPI.create() → POST /api/projects → Supabase → redirect to HomePage → ProjectCard renders the new project. Phase 1 Walking Skeleton is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → React form | All form values are user-supplied; validated client-side before submission |
| React form → FastAPI /api/projects | JSON body — FastAPI validates with Pydantic; Vite proxy handles routing |
| API response → React state | ProjectResponse — has_api_key bool, never key plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-key-ui | Information Disclosure | ProjectFormPage API key field | mitigate | Edit mode shows only "●●●●●●●●●●" (10 bullets). The actual key is never returned by GET /projects/:id — has_api_key bool only. Frontend cannot display what it doesn't receive. |
| T-01-supabase-browser | Information Disclosure | frontend/src/lib/supabase.ts | mitigate | File exports a function but it is NOT called anywhere in Phase 1. No SUPABASE_KEY (service role) is ever put in a frontend .env file. VITE_SUPABASE_ANON_KEY would be the anon key only (much lower privilege) — but even that is not needed in Phase 1. |
| T-01-xss | Tampering | React JSX rendering | accept | React escapes all string values in JSX by default. project.name, project.client etc. are rendered as text nodes. No dangerouslySetInnerHTML usage in Phase 1 UI. |
| T-01-client-validation | Tampering | ProjectFormPage validation | accept | Client-side validation is UX only — the real validation gate is the Pydantic model on the backend. Missing a client-side check is a UX regression, not a security issue. |
| T-01-SC | Tampering | npm install (Plan 01b) | mitigate | No new npm packages added in Plan 03. All packages installed in Plan 01b are Approved. |
</threat_model>

<verification>
After all 3 tasks complete:

1. TypeScript compiles: `cd frontend && npx tsc --noEmit` → exit 0, no errors
2. Build succeeds: `npm run build` → exit 0
3. No hardcoded hex: `grep -rE "#[0-9a-fA-F]{3,6}" frontend/src/` returns only comments or @theme definitions in index.css — not component files
4. Dev server smoke test (requires backend running):
   a. `npm run dev` → localhost:5173 opens
   b. Home page shows "Nenhum projeto ainda" (empty state)
   c. Navigate to /projects/new → form with 4 sections renders
   d. Submit form with all required fields → redirects to home → new project card appears
   e. Click card → navigate to /projects/:id
   f. Click "Editar projeto" → form pre-filled → API key shows "●●●●●●●●●●"
   g. Click "Alterar chave" → key input re-enables
5. "ao vivo" badge: insert active session via Supabase Dashboard or psql → reload home page → badge appears on project card
</verification>

<success_criteria>
- Complete vertical slice works: form submit → POST /api/projects → Supabase → project card on home screen (PROJ-01)
- Edit flow works: pre-filled form, API key masked, Alterar chave re-enables input (PROJ-02, PROJ-04)
- Active session badge appears (has_active_session=true in API response → "ao vivo" badge in ProjectCard) (PROJ-03)
- API key never displayed in the UI — only has_api_key bool drives masking (PROJ-04)
- All UI text matches Portuguese Copywriting Contract from UI-SPEC
- No hardcoded hex values in component or page files
- `npm run build` exits 0
</success_criteria>

<output>
Create `.planning/phases/01-project-configuration-supabase-schema/01-03-SUMMARY.md` when done.
</output>

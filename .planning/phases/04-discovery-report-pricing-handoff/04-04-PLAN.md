---
phase: 04-discovery-report-pricing-handoff
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - backend/app/routers/sessions.py
  - backend/tests/test_upload_pdf_mode.py
autonomous: true
requirements: [REP-01, REP-03]
estimate:
  tokens: 30000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "REP-01: upload_pdf_transcript propaga mode=project.get('mode','sales') para generate_report — um PDF de sessão discovery gera relatório no formato PRD, não sales (corrige a inconsistência silenciosa D-39/Pitfall 4)"
    - "REP-01: o INSERT de reports do upload de PDF grava status='Rascunho' quando mode=='discovery' (mesmo grant do pipeline, D-36), NULL para sales"
    - "REP-03: sessões sales via upload de PDF continuam gerando relatório sales (mode default 'sales'); comportamento sales inalterado"
  artifacts:
    - "backend/app/routers/sessions.py::upload_pdf_transcript (parâmetro mode= propagado + grant de status discovery)"
    - backend/tests/test_upload_pdf_mode.py
  key_links:
    - "upload_pdf_transcript: project (já no escopo, linha ~369) -> generate_report(mode=project.get('mode','sales')) -> INSERT reports com status='Rascunho' se discovery"
  prohibitions:
    - statement: "Esta correção (D-39) NÃO pode ser misturada com feature/refator no mesmo commit — é uma correção isolada (regra CLAUDE.md: nunca misturar correção com feature)"
      verification: "flagged-unverified — este plano é um único commit dedicado à correção D-39; nenhuma outra feature entra aqui"
    - statement: "A correção NÃO pode regredir o caminho sales de upload de PDF — mode default 'sales' preserva o comportamento atual (REP-03)"
      verification: "test_upload_pdf_mode.py assere que projeto sem mode (ou mode='sales') gera relatório no formato sales"
---

<objective>
Correção isolada (D-39): o endpoint `POST /sessions/{session_id}/transcript/upload` chama
`generate_report(...)` SEM o parâmetro `mode=`, então o default `"sales"` faz um PDF de reunião
discovery gerar relatório sales silenciosamente (Pitfall 4 do RESEARCH). Este plano propaga
`mode=project.get("mode","sales")` e concede `status='Rascunho'` no INSERT quando discovery — nada
mais. É uma CORREÇÃO, e por regra do CLAUDE.md deve ser um commit SEPARADO da feature do relatório
(04-02).

Purpose: fechar a inconsistência silenciosa do caminho de upload de PDF sem tocar nenhuma outra
lógica — um discovery importado por PDF passa a gerar o PRD, e o grant de status fica coerente com o
pipeline ao vivo.
Output: uma linha de `mode=` propagada + o grant condicional de status no INSERT do upload + um teste
que prova discovery vs sales no upload.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-discovery-report-pricing-handoff/04-CONTEXT.md
@.planning/phases/04-discovery-report-pricing-handoff/04-RESEARCH.md
@.planning/phases/04-discovery-report-pricing-handoff/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Símbolos alterados/novos entregues por ESTE plano:
- **Correção:** `upload_pdf_transcript` passa `mode=project.get("mode","sales")` na chamada de
  `generate_report` (`backend/app/routers/sessions.py`, ~469-479).
- **Grant coerente:** `{"status":"Rascunho"}` no `db.table("reports").insert({...})` do upload quando
  `project.get("mode")=="discovery"` (~484-491), mesmo padrão do pipeline (04-01).
- **Arquivo de teste novo:** `backend/tests/test_upload_pdf_mode.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (CORREÇÃO ISOLADA D-39): propagar mode no upload_pdf_transcript + grant de status coerente</name>
  <files>backend/app/routers/sessions.py, backend/tests/test_upload_pdf_mode.py</files>
  <read_first>
    - backend/app/routers/sessions.py (upload_pdf_transcript 345-499; chamada generate_report 469-479; INSERT reports 484-491; project no escopo linha 369)
    - backend/app/services/llm.py (assinatura generate_report 102-114 — parâmetro mode default 'sales')
    - .planning/phases/04-discovery-report-pricing-handoff/04-RESEARCH.md (Pitfall 4; A4)
    - .planning/phases/04-discovery-report-pricing-handoff/04-PATTERNS.md (bloco upload_pdf_transcript / fix D-39, linhas 251-267)
  </read_first>
  <behavior>
    - Projeto com `mode='discovery'`: `generate_report` é chamado com `mode='discovery'` e o INSERT grava `status='Rascunho'`.
    - Projeto com `mode='sales'` ou sem mode: `generate_report` recebe `mode='sales'` (default) e o INSERT NÃO grava status (NULL) — comportamento sales atual, inalterado (REP-03).
  </behavior>
  <action>
    Mudança mínima e isolada (um único commit dedicado — CLAUDE.md: correção nunca no mesmo commit que
    feature/refator):
    1. Na chamada `markdown, inp, out = await llm_service.generate_report(...)` de `upload_pdf_transcript`
       (~469-479), acrescentar o argumento `mode=project.get("mode", "sales")`. `project` já está no
       escopo (linha ~369, `project = session.get("projects") or {}`).
    2. No `db.table("reports").insert({...})` do upload (~484-491), acrescentar
       `**({"status": "Rascunho"} if project.get("mode") == "discovery" else {})}` ao dict — mesmo grant
       condicional por mode do pipeline (04-01), para o relatório discovery via PDF nascer 'Rascunho'.
    Não alterar mais nada neste endpoint (a partição por lens de perguntas/red_flags do caminho de PDF
    fica FORA do escopo desta correção — as seleções do DB do upload não trazem lens; isso é uma
    limitação aceita e registrada, não parte do fix D-39).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_upload_pdf_mode.py -x -q</automated>
    <fails_when>exit não-zero, ou "0 passed"/"failed"/"error" na linha de sumário do pytest</fails_when>
  </verify>
  <verify>
    <automated>cd backend && python -c "import re; s=open('app/routers/sessions.py',encoding='utf-8').read(); i=s.index('def upload_pdf_transcript'); body=s[i:i+6000]; assert 'mode=project.get(\"mode\"' in body, 'mode nao propagado no upload'; print('UPLOAD_MODE_OK')"</automated>
    <fails_when>AssertionError ou "UPLOAD_MODE_OK" não impresso — a chamada de generate_report no upload não propaga mode</fails_when>
  </verify>
  <acceptance_criteria>
    - `upload_pdf_transcript` passa `mode=project.get("mode","sales")` para `generate_report`.
    - O INSERT do upload grava `status='Rascunho'` só quando discovery; sales fica NULL.
    - `test_upload_pdf_mode.py` prova os dois caminhos (discovery gera PRD/status; sales gera sales/None).
    - Um único commit, dedicado à correção D-39 (sem feature/refator junto).
  </acceptance_criteria>
  <done>Upload de PDF de sessão discovery gera relatório PRD com status 'Rascunho'; sales inalterado; correção isolada em um commit.</done>
</task>

</tasks>

<planejamento_task>
## Plano da task — 7 seções obrigatórias (CLAUDE.md)

1. **O que muda (linguagem comum):** hoje, se você sobe um PDF da transcrição de uma reunião de
   discovery, o sistema gera por engano o relatório de VENDA, porque esse caminho esqueceu de avisar
   qual é o modo. A correção passa a avisar o modo certo — PDF de discovery vira relatório de PRD, PDF
   de venda continua venda. É consertar um "esqueci de dizer que é discovery".
2. **Como vai ser alterado:** uma linha na chamada que gera o relatório (`mode=...`) e uma linha no
   registro do relatório (carimbo 'Rascunho' só quando discovery), no arquivo `sessions.py`. Um único
   commit, só a correção.
3. **Riscos de cada ação:** propagar o modo errado poderia mudar o relatório de venda (regressão);
   gravar status para venda sujaria o gate. Ambos evitados porque o default é 'sales' e o carimbo é
   condicional a discovery.
4. **Como prevenir:** teste que roda os dois cenários (projeto discovery vs. projeto sem modo/venda);
   grep confirmando a propagação; default 'sales' preserva o caminho atual.
5. **Como consertar (rollback):** `git revert` do único commit restaura o comportamento anterior
   (relatório de venda no upload) — sem efeito colateral, porque foi uma correção isolada.
6. **Decisões que são do time:** a partição por lens do caminho de PDF (perguntas/red flags sem lens
   nas seleções do banco) fica FORA do escopo — registrada como limitação aceita. Se o time quiser o
   caminho de PDF totalmente lens-aware, é DECISÃO EM ABERTO para outra task.
7. **Verificação (ponta a ponta):** `test_upload_pdf_mode.py` prova discovery→PRD/'Rascunho' e
   sales→sales/None; o grep garante a propagação do modo.
</planejamento_task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (upload de PDF) | arquivo PDF é input não-confiável (já validado por extensão/extração no endpoint atual) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | upload_pdf_transcript (grant de status) | low | mitigate | Grant condicional por `mode` (mesmo padrão do pipeline); sales nunca ganha status; nenhum input de usuário decide o status |
| T-04-08 | Information Disclosure | relatório gerado do PDF | low | accept | Conteúdo do relatório; validação de extração do PDF já existe no endpoint (não alterada) |
| T-04-SC | Tampering (supply chain) | dependências | low | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- Suíte alvo do plano: `cd backend && python -m pytest tests/test_upload_pdf_mode.py -x -q`
- Regressão de wave: `cd backend && python -m pytest tests/ -q`
- Correção isolada (D-39) num único commit, separada da feature (04-02).
</verification>

<success_criteria>
- Upload de PDF discovery gera relatório PRD com `status='Rascunho'`; sales gera sales com status NULL.
- REP-03 preservado no caminho de upload (default 'sales').
- Suíte backend verde.
</success_criteria>

<output>
Create `.planning/phases/04-discovery-report-pricing-handoff/04-04-SUMMARY.md` when done
</output>

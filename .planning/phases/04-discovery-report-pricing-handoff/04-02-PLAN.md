---
phase: 04-discovery-report-pricing-handoff
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - backend/tests/test_discovery_report.py
  - backend/app/services/discovery_prompt_builder.py
  - backend/app/services/llm.py
  - backend/app/services/pipeline.py
autonomous: true
requirements: [REP-01, REP-03]
estimate:
  tokens: 62000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "REP-01: generate_report(mode='discovery') produz o esqueleto do PRD de 16 seções (0-15) via build_report_generator reescrito, preenchendo apenas o que o pipeline capturou; subseções sem insumo ficam marcadas '[a preencher no PRD]' (D-26)"
    - "REP-01: a mensagem 'user' do ramo discovery traz DUAS tabelas de cobertura — uma Produto, uma Dados — com labels do DISCOVERY_AREA_SET (não do SALES_AREA_SET), particionadas por lens (D-27/D-28)"
    - "REP-01: perguntas usadas chegam ao relatório particionadas por lens (questions_used vira list[dict] {text,lens}, mesmo padrão de red_flags_raw) para a seção 12.4 'Dúvidas em aberto' (D-40)"
    - "REP-01 (contrato cross-plan com 04-04): o ramo discovery normaliza defensivamente cada item ANTES de agrupar — questions_used e red_flags aceitam tanto list[dict] (caminho vivo, com lens) quanto list[str]/dict-sem-lens (caminho upload_pdf_transcript de sessions.py ~440-455, NÃO modificado por nenhum plano); itens sem lens caem num bucket 'não classificado' e NUNCA levantam AttributeError (D-39/D-40, mesmo espírito da normalização já feita no ramo sales)"
    - "REP-03: generate_report(mode='sales') é byte-idêntico ao comportamento pré-fase (bloco CITi presente, mesma ordem, questions_used aceitando list[str] legado) — golden congelado ANTES de tocar llm.py"
    - "DISC-03: o builder discovery NUNCA importa CITI_PORTFOLIO/CITI_SERVICE_CATALOG/CITI_TECH_REFERENCE"
  artifacts:
    - "backend/app/services/discovery_prompt_builder.py::build_report_generator (reescrito para o esqueleto PRD de 16 seções)"
    - "backend/app/services/llm.py::generate_report (ramo discovery: 2 tabelas por lens + questions por lens)"
    - "backend/app/services/llm.py::_coverage_table_for_lens (helper novo de pré-montagem por lens)"
    - "backend/app/services/pipeline.py::_run_report_generator (questions_used -> list[dict] com lens, D-40)"
    - backend/tests/test_discovery_report.py
  key_links:
    - "SessionState.coverage_to_dict() (já traz lens por item) -> _coverage_table_for_lens('produto'|'dados') -> 2 tabelas na 'user' message -> build_report_generator() PRD skeleton como system prompt -> Markdown com seções Produto/Dados"
  prohibitions:
    - statement: "O ramo discovery NÃO pode alterar a saída sales — sales continua usando SALES_AREA_SET, o bloco CITi e a estrutura de hoje (REP-03 byte-identidade)"
      verification: "test_discovery_report.py::test_sales_mode_unchanged compara a 'user' message sales contra fixture congelada capturada antes da mudança"
    - statement: "O ramo discovery NÃO pode usar SALES_AREA_SET.labels() (bug de llm.py:121) — deve usar DISCOVERY_AREA_SET por lens"
      verification: "test_discovery_report.py assere que labels de DISCOVERY_AREA_SET (ex.: 'Gargalo','Métricas') aparecem no ramo discovery"
    - statement: "O LLM NÃO pode decidir a lens de um item já classificado — a partição por lens é determinística no código (D-27), o registro é autoritário"
      verification: "flagged-unverified — a partição usa info['lens'] do coverage_to_dict; o prompt só recebe as tabelas já montadas"
    - statement: "O ramo discovery NÃO pode lançar exceção ao consumir as shapes REAIS de upload_pdf_transcript — questions_used como list[str] e red_flags como list[dict] SEM a coluna lens (sessions.py ~440-455). Um q.get('lens') num str levantaria AttributeError e quebraria a geração inteira do relatório justo no caminho que 04-04 exercita (D-39)"
      verification: "test_discovery_report.py::test_discovery_tolerates_lensless_upload_shape chama generate_report(mode='discovery') com questions_used=list[str] + red_flags=list[dict sem lens] e assere que NÃO há exceção e que os itens aparecem sob o bucket 'não classificado'"
---

<objective>
Expandir a fatia provada no 04-01 para o corpo REAL do relatório discovery (REP-01): um relatório
IRMÃO escolhido por `mode` (D-25) — o esqueleto do PRD de 16 seções (0-15) como system prompt
(reescrevendo `build_report_generator`), e a pré-montagem híbrida código+LLM na `generate_report` —
duas tabelas de cobertura (Produto/Dados) com labels do `DISCOVERY_AREA_SET`, red flags por lens e
perguntas por lens (D-40). As métricas de precificação ficam nativas no PRD (§6.3/§7.3/§11), sem
seção nomeada (D-29) — o SC#1 do ROADMAP foi reformulado por D-30 para refletir isso. Sales permanece
byte-idêntico (REP-03), garantido por um golden congelado ANTES de qualquer mudança em `llm.py`.

Purpose: entregar o relatório discovery que segue fielmente o PRD padrão da CITi, preenchendo só o
que o pipeline capturou e marcando `[a preencher no PRD]` no resto (D-26) — sem regredir sales.
Output: `build_report_generator` reescrito, ramo discovery em `generate_report`, `questions_used`
como `list[dict]`, e o golden sales + testes de estrutura discovery.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-discovery-report-pricing-handoff/04-CONTEXT.md
@.planning/phases/04-discovery-report-pricing-handoff/04-RESEARCH.md
@.planning/phases/04-discovery-report-pricing-handoff/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Símbolos novos/alterados entregues por ESTE plano:
- **Reescrita:** `discovery_prompt_builder.build_report_generator()` → esqueleto PRD de 16 seções
  (0-15), com marcador `[a preencher no PRD]`, calibrado por DMS (`self._dms_str()`), sem imports CITi.
- **Função nova:** `llm._coverage_table_for_lens(coverage, lens)` — pré-montagem determinística de tabela por lens.
- **Ramo novo:** bloco `if mode == "discovery"` em `generate_report` (2 tabelas por lens + red flags
  por lens + perguntas por lens na 'user' message).
- **Mudança de assinatura interna:** `questions_used` aceita `list[dict]` `{text,lens}` (retrocompatível
  com `list[str]` no ramo sales) em `generate_report` e em `pipeline._run_report_generator`.
- **Normalização defensiva (contrato cross-plan com 04-04):** o ramo discovery de `generate_report`
  normaliza cada item de `questions_used`/`red_flags` ANTES de agrupar (isinstance-guard), tolerando o
  shape lens-less que `upload_pdf_transcript` entrega (list[str] + dicts sem `lens`) — itens sem lens
  vão para o bucket "não classificado", nunca para um `AttributeError` (D-39).
- **Arquivo de teste novo:** `backend/tests/test_discovery_report.py` (golden sales REP-03 + estrutura discovery REP-01).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Congelar o golden sales (REP-03) ANTES de tocar llm.py + criar o arquivo de teste</name>
  <files>backend/tests/test_discovery_report.py</files>
  <read_first>
    - backend/tests/test_discovery_mode.py (_capture_report_user 166-186; testes sales/discovery 189-223)
    - backend/app/services/llm.py (generate_report 102-195; montagem da 'user' message 185-193)
    - .planning/phases/04-discovery-report-pricing-handoff/04-PATTERNS.md (bloco test_discovery_report.py)
  </read_first>
  <action>
    Criar `backend/tests/test_discovery_report.py` reaproveitando o helper `_capture_report_user`
    (monkeypatch de `llm._call`, captura a 'user' message) do analog `test_discovery_mode.py`. Este é
    o PRIMEIRO passo do plano e deve rodar ANTES de qualquer edição em `llm.py`, para congelar a saída
    sales como referência (REP-03). Escrever:
    - `test_sales_mode_unchanged(monkeypatch)`: captura `_capture_report_user(monkeypatch, mode="sales")`
      e compara contra uma fixture congelada da saída ATUAL (incluir o cabeçalho comercial CITi e a
      ordem: índice de `## Alertas detectados` menor que o do bloco CITi, menor que o de
      `## Transcrição completa`). Congelar a fixture com a saída de HOJE — é a linha de defesa de byte-identidade.
    - `test_sales_default_mode_equals_sales(monkeypatch)`: `mode` omitido igual a `mode="sales"`.
    Rodar a suíte e confirmar VERDE agora (antes de mudar produção) — o golden nasce passando.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_discovery_report.py::test_sales_mode_unchanged tests/test_discovery_report.py::test_sales_default_mode_equals_sales -x -q</automated>
    <fails_when>exit não-zero, ou "0 passed"/"failed" na linha de sumário — o golden sales não foi congelado corretamente</fails_when>
  </verify>
  <acceptance_criteria>
    - `test_discovery_report.py` existe e o golden sales passa ANTES de qualquer edição em `llm.py`.
    - O golden cobre a presença do bloco CITi e a ordem das seções na 'user' message sales.
  </acceptance_criteria>
  <done>Golden sales congelado e verde; qualquer regressão futura em sales será detectada por este teste.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescrever build_report_generator para o esqueleto do PRD de 16 seções (D-26)</name>
  <files>backend/app/services/discovery_prompt_builder.py</files>
  <read_first>
    - backend/app/services/discovery_prompt_builder.py (build_report_generator atual 247-273; _dms_str 69-72; docstring/guarda-corrilho do topo 1-38)
    - .planning/phases/04-discovery-report-pricing-handoff/04-RESEARCH.md ("PRD Skeleton (16 seções, 0-15)" — títulos/subtítulos verbatim; mapa de lens por seção)
    - .planning/phases/04-discovery-report-pricing-handoff/04-CONTEXT.md (D-26, D-27, D-29; mapeamento Produto->1-6,10,11; Dados->7 + partes de 8,9)
  </read_first>
  <behavior>
    - O system prompt retornado contém os títulos das 16 seções do PRD (0 a 15) na ordem do RESEARCH
      (ex.: "Sumário executivo", "Requisitos de dados", "Priorização & faseamento", "Riscos, premissas,
      dependências & dúvidas em aberto").
    - Instrui o LLM a preencher SOMENTE o que vier nas tabelas/listas do 'user' message e a marcar
      o placeholder de seção vazia em qualquer subseção sem dado (D-26).
    - Mantém a calibração por DMS via `self._dms_str()`.
    - NÃO contém nem importa nenhum dos três símbolos comerciais do sales (DISC-03).
    - Instrui o LLM a copiar as DUAS tabelas de cobertura (Produto/Dados) fornecidas no 'user' sem alterá-las.
  </behavior>
  <action>
    Reescrever inteiramente `build_report_generator(self) -> str` (assinatura mantida, sem novos
    parâmetros) para retornar o esqueleto do PRD de 16 seções (0-15) conforme o "PRD Skeleton" do
    RESEARCH — usar os títulos/subtítulos como esqueleto de seções, instruindo o LLM a: (1) seguir a
    ordem exata das 16 seções; (2) preencher cada seção só com o conteúdo derivável das tabelas/listas
    do 'user' message (cobertura por lens, red flags por lens, perguntas por lens, transcrição,
    contexto); (3) usar o marcador de subseção vazia (o marcador exato é discricionário, D-26, mas deve
    ser consistente e informado ao LLM) em toda subseção sem insumo — seções 4, 5, 10 e partes de
    2.4/12.4 ficarão majoritariamente vazias, o que é esperado por D-26; (4) mapear Produto -> seções
    1-6,10,11 e Dados -> seção 7 + partes de 8 (LGPD/observabilidade) e 9 (arquitetura/integrações);
    (5) posicionar os sinais nativos de precificação nas seções do PRD (6.3 backlog+estimativas, 7.3
    volumetria, 11 faseamento), SEM criar uma seção nomeada de métricas (D-29). Preservar o
    guarda-corrilho do topo do módulo (nada de símbolos comerciais). Comentar as decisões (D-26/D-27/D-29) inline.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.services.discovery_prompt_builder import DiscoveryPromptBuilder; p=DiscoveryPromptBuilder(dms=3).build_report_generator(); assert 'Sumário executivo' in p and 'Requisitos de dados' in p and 'faseamento' in p.lower(); print('PRD_SKELETON_OK')"</automated>
    <fails_when>AssertionError ou "PRD_SKELETON_OK" não impresso — faltam títulos do esqueleto PRD</fails_when>
  </verify>
  <verify>
    <automated>cd backend && python -c "import app.services.discovery_prompt_builder as m; src=open(m.__file__,encoding='utf-8').read(); assert 'CITI_PORTFOLIO' not in src and 'CITI_SERVICE_CATALOG' not in src and 'CITI_TECH_REFERENCE' not in src; print('NO_CITI_IMPORT_OK')"</automated>
    <fails_when>AssertionError ou "NO_CITI_IMPORT_OK" não impresso — o builder discovery referencia símbolo comercial (viola DISC-03)</fails_when>
  </verify>
  <acceptance_criteria>
    - `build_report_generator()` retorna o esqueleto das 16 seções na ordem do PRD, com marcador de seção vazia e calibração DMS.
    - O módulo não referencia nenhum dos três símbolos comerciais do sales (DISC-03 preservado).
  </acceptance_criteria>
  <done>System prompt discovery = esqueleto PRD de 16 seções; DISC-03 intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ramo discovery em generate_report — 2 tabelas por lens + questions por lens (D-27/D-28/D-40)</name>
  <files>backend/app/services/llm.py, backend/app/services/pipeline.py, backend/tests/test_discovery_report.py</files>
  <read_first>
    - backend/app/services/llm.py (generate_report 102-195; area_labels/coverage_table 121-139; citi_block 179-183; user message 185-193; imports 1-8)
    - backend/app/services/coverage_areas.py (DISCOVERY_AREA_SET 91-115; by_lens 61-68; labels 47-48)
    - backend/app/services/pipeline.py (_run_report_generator 434-478; red_flags_raw 442-450; questions_used 439-441; comentário-guarda 411-413)
    - backend/app/services/session_state.py (coverage_to_dict 156-173 — já injeta 'lens' por item)
    - .planning/phases/04-discovery-report-pricing-handoff/04-PATTERNS.md (blocos de llm.py e pipeline.py; _coverage_table_for_lens)
  </read_first>
  <behavior>
    - Sales (mode='sales' ou omitido): 'user' message byte-idêntica ao golden da Task 1 (SALES_AREA_SET, bloco CITi, mesma ordem).
    - Discovery (mode='discovery'): 'user' traz duas tabelas — uma com labels Produto (ex.: 'Gargalo'),
      outra com labels Dados (ex.: 'Métricas') — filtradas por `info['lens']`, e a lista de perguntas separada por lens.
    - `questions_used` como `list[dict]` {text,lens} é aceito; `list[str]` legado continua aceito no ramo sales.
    - Robustez de shape (contrato com 04-04): no ramo discovery, `questions_used` aceita list[dict] {text,lens}
      OU list[str], e `red_flags` aceita list[dict] com OU sem `lens`; itens sem lens vão para o bucket
      "não classificado" sem levantar exceção — é exatamente o shape que `upload_pdf_transcript` entrega
      (list[str] de perguntas + dicts de red_flags sem coluna lens).
  </behavior>
  <action>
    1. **Import (llm.py):** acrescentar `DISCOVERY_AREA_SET` ao import de `app.services.coverage_areas`
       (manter `SALES_AREA_SET` — sales continua usando ele).
    2. **Helper novo `_coverage_table_for_lens(coverage, lens)`:** seguir o formato de tabela de
       `coverage_rows`/`coverage_table` (llm.py:123-139), mas filtrando `if isinstance(info, dict) and
       info.get("lens") == lens` (o `coverage_to_dict` já injeta `lens` por item — mais barato que
       recalcular `by_lens()`); usar `info.get("name") or area` como label e `DISCOVERY_AREA_SET` como
       fallback de label quando `name` vier vazio. Retornar `"_Nenhuma área classificada ainda._"` quando não houver linhas.
    3. **Ramo discovery em generate_report:** dentro de `if mode == "discovery"`, montar a 'user'
       message com DUAS tabelas (`_coverage_table_for_lens(coverage,'produto')` e `(...,'dados')`),
       red flags agrupados por lens, e perguntas agrupadas por lens (seção 12.4 do PRD). Ao agrupar
       red flags E perguntas, extrair a lens DEFENSIVAMENTE: `lens = item.get("lens") if isinstance(item,
       dict) else None` — itens que não são dict, ou dicts sem a chave `lens` (o shape que
       `upload_pdf_transcript` entrega: red_flags de um SELECT text/severity/evidence SEM coluna lens),
       caem num bucket "não classificado", NUNCA num `AttributeError`. O `citi_block` já é `"" if mode !=
       "sales"` — manter. O ramo sales permanece EXATAMENTE como hoje (não editar a montagem sales).
       Gatear SEMPRE por `mode`, nunca por um segundo flag (comentário-guarda de pipeline.py:411-413).
    4. **questions_used retrocompatível (D-40 + contrato cross-plan com 04-04):** aceitar `list[dict]`
       {text,lens} e `list[str]`. No ramo sales, normalizar com `q["text"] if isinstance(q, dict) else q`
       (preserva byte-identidade sales). No ramo discovery, normalizar ANTES de particionar:
       `text = q.get("text") if isinstance(q, dict) else q` e `lens = q.get("lens") if isinstance(q, dict)
       else None` — assim um `questions_used` list[str] vindo de `upload_pdf_transcript` (sessions.py ~455)
       cai no bucket "não classificado" em vez de estourar `AttributeError` em `q.get("lens")` sobre um str.
    5. **pipeline._run_report_generator (D-40):** trocar `questions_used = [q.text for q ...]` por
       `questions_used = [{"text": q.text, "lens": q.lens} for q in self.state.questions if q.status in
       ("used","pinned")]` — mesmo padrão de `red_flags_raw`. NÃO tocar no grant de status (feito no 04-01).
    6. Escrever em `test_discovery_report.py` os testes REP-01: `test_discovery_two_lens_tables`
       (passa coverage com um item 'produto' e um 'dados', assere que ambos os labels aparecem na 'user'
       discovery e que uma label sales-only não aparece), `test_discovery_marks_empty_sections`
       (assere que o marcador de seção vazia aparece via o system prompt discovery quando sem insumo) e
       `test_discovery_tolerates_lensless_upload_shape` (teste-semente do contrato cross-plan com 04-04:
       chama `generate_report(mode='discovery')` com `questions_used=list[str]` e `red_flags=list[dict SEM
       lens]` — exatamente o shape de `upload_pdf_transcript` — e assere que NÃO há exceção e que os itens
       aparecem sob o bucket "não classificado" na 'user' message).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_discovery_report.py -x -q</automated>
    <fails_when>exit não-zero, ou "0 passed"/"failed"/"error" na linha de sumário do pytest</fails_when>
  </verify>
  <verify>
    <automated>cd backend && python -c "import inspect; from app.services import llm; assert hasattr(llm,'_coverage_table_for_lens'); src=inspect.getsource(llm.generate_report); assert 'DISCOVERY_AREA_SET' in open(llm.__file__,encoding='utf-8').read(); print('DISCOVERY_BRANCH_OK')"</automated>
    <fails_when>AttributeError/AssertionError ou "DISCOVERY_BRANCH_OK" não impresso — helper por lens ou uso de DISCOVERY_AREA_SET ausente</fails_when>
  </verify>
  <verify>
    <automated>cd backend && python -c "s=open('app/services/pipeline.py',encoding='utf-8').read(); assert '{\"text\": q.text, \"lens\": q.lens}' in s or ('q.text' in s and 'q.lens' in s and 'questions_used' in s); print('QUESTIONS_LIST_DICT_OK')"</automated>
    <fails_when>AssertionError ou "QUESTIONS_LIST_DICT_OK" não impresso — questions_used ainda é list[str] plano no pipeline (D-40 não aplicado)</fails_when>
  </verify>
  <acceptance_criteria>
    - Suíte `test_discovery_report.py` verde: golden sales inalterado + duas tabelas por lens no discovery + marcador de seção vazia.
    - `generate_report` usa `DISCOVERY_AREA_SET` só no ramo discovery; sales continua com `SALES_AREA_SET` e byte-idêntico.
    - `questions_used` é `list[dict]` {text,lens} no pipeline; `generate_report` aceita as duas formas.
    - O ramo discovery NÃO estoura com o shape lens-less de `upload_pdf_transcript` (questions=list[str], red_flags=dicts sem lens): `test_discovery_tolerates_lensless_upload_shape` verde, itens no bucket "não classificado" (contrato com 04-04, D-39).
  </acceptance_criteria>
  <done>Relatório discovery segue o esqueleto PRD com duas tabelas por lens e perguntas por lens; sales byte-idêntico (golden verde).</done>
</task>

</tasks>

<planejamento_task>
## Plano da task — 7 seções obrigatórias (CLAUDE.md)

1. **O que muda (linguagem comum):** hoje o relatório de discovery é um resumo simples de 6 seções.
   Passa a ser o documento completo no formato do PRD padrão da CITi (16 seções), preenchendo só o que
   a reunião realmente capturou e escrevendo "a preencher" no resto. As áreas cobertas viram duas
   tabelas separadas — uma de Produto, outra de Dados — e as perguntas em aberto aparecem separadas por
   essas duas lentes. O relatório de venda não muda em nada.
2. **Como vai ser alterado:** Task 1 congela o relatório de venda atual num teste (rede de segurança)
   ANTES de mexer no código; Task 2 reescreve o "molde" do relatório discovery (o system prompt) para
   as 16 seções; Task 3 monta as duas tabelas por lente e as perguntas por lente no código. Commits
   atômicos: 1 teste-golden, 1 molde PRD, 1 pré-montagem por lens.
3. **Riscos de cada ação:** mexer em `llm.py` pode regredir o relatório de venda (quebra REP-03);
   usar o conjunto de áreas errado (SALES em vez de DISCOVERY) mostra as 8 áreas erradas; mudar
   `questions_used` para dict pode quebrar quem ainda passa lista de strings (o upload de PDF); o PRD
   de 16 seções é mais longo que o relatório sales de 7 e pode ESTOURAR o teto
   `REPORT_MAX_OUTPUT_TOKENS = 16384` (llm.py:21), truncando o Markdown no meio (RESEARCH Pitfall 3 —
   ASSUNÇÃO monitorada, não decisão de escopo).
4. **Como prevenir:** golden de venda congelado na Task 1 (roda antes e depois); teste que confere as
   labels Produto/Dados no ramo discovery; assinatura de `questions_used` retrocompatível (aceita dict
   e str) — o upload de PDF continua passando strings sem quebrar. Para o teto de tokens: D-26 já
   mitiga (só preenche o que o pipeline capturou → menos texto); se um relatório real truncar, subir
   `REPORT_MAX_OUTPUT_TOKENS` SÓ no ramo discovery (sales mantém o valor de hoje) — registrar como
   follow-up se observado, sem alterar o teto às cegas nesta task.
5. **Como consertar (rollback):** `git revert` do commit da Task 3 restaura a montagem anterior de
   `generate_report`; revert da Task 2 restaura o molde de 6 seções; o golden da Task 1 fica como
   detector permanente de regressão. Sales nunca é tocado, volta sozinho.
6. **Decisões que são do time:** o formato exato do marcador de seção vazia e do esqueleto Markdown é
   discricionário (D-26) — sem decisão em aberto aqui. Se o esqueleto precisar de seções ADICIONAIS ao
   PRD, PARAR e perguntar (fora do escopo).
7. **Verificação (ponta a ponta):** `test_discovery_report.py` cobre sales (golden) e discovery
   (estrutura); a fidelidade da prosa gerada por LLM real é verificação manual (VALIDATION.md).
</planejamento_task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline → LLM (generate_report) | transcrição/cobertura entram no prompt; conteúdo, não superfície de código executável |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Information Disclosure | seção 7.5 LGPD do PRD | low | accept | Fora de mitigação ativa (é conteúdo, não vulnerabilidade); o esqueleto reserva a seção 7.5 e não a omite mesmo vazia (RESEARCH Security Domain) |
| T-04-04 | Tampering | ramo discovery de generate_report | low | mitigate | Partição por lens é determinística no código (registro autoritário, D-27); o LLM não reclassifica itens já taggeados |
| T-04-SC | Tampering (supply chain) | dependências | low | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- Suíte alvo do plano: `cd backend && python -m pytest tests/test_discovery_report.py -x -q`
- Regressão de wave: `cd backend && python -m pytest tests/ -q`
- Golden sales (REP-03) congelado antes de tocar `llm.py`.
</verification>

<success_criteria>
- Relatório discovery segue o esqueleto PRD de 16 seções com duas tabelas por lens e perguntas por lens.
- Sales byte-idêntico (golden verde); DISC-03 preservado (sem símbolos comerciais no builder discovery).
- Suíte backend verde.
</success_criteria>

<output>
Create `.planning/phases/04-discovery-report-pricing-handoff/04-02-SUMMARY.md` when done
</output>
